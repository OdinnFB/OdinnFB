<!DOCTYPE html>
<html>
<head>
<title>E-waste Archaeology</title>
<style>
html { color-scheme: light dark; }
body { max-width: 50em; margin: 0 auto; padding: 0 1rem;
font-family: Tahoma, Verdana, Arial, sans-serif; }
#bg-video {
  position: fixed;
  top: 50%;
  left: 50%;
  /* center the element */
  transform: translate(-50%, -50%) rotate(90deg) scale(0.9);
  transform-origin: center center;
  /* ensure it fills the viewport after rotation */
  min-width: 100vh;
  min-height: 100vw;
  width: auto;
  height: auto;
  z-index: -1;
  opacity: 0.18;
  object-fit: cover;
}
.content {
  position: relative;
  z-index: 1;
  max-width: 100rem;
  margin: 0 auto;
  padding: 2rem 1.2rem;
}
/* Double the size of top text */
.content h1 {
  font-size: 3.2rem;
}
.content em {
  font-size: 2rem;
  display: block;
  margin-bottom: 1.5rem;
}
/* gallery at page bottom */
.gallery {
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 2.7rem; /* 80% of 3.375rem */
  max-width: 80rem;
  margin: 3.2rem auto 6rem;
  padding: 0 2.4rem;
}
.gallery-row {
  display: flex;
  flex-direction: row;
  gap: 1.8rem; /* 80% of 2.25rem */
  align-items: flex-start;
  justify-content: center;
  width: 100%;
}
.gallery-row img:first-child {
  height: auto;
  width: auto;
  max-height: 504px; /* 80% of 630px */
  object-fit: contain;
  border-radius: 9.6px;
  box-shadow: 0 9.6px 28.8px rgba(0,0,0,0.18);
}
.gallery-row img:last-child {
  height: auto;
  width: auto;
  max-height: 504px;
  object-fit: contain;
  border-radius: 9.6px;
  box-shadow: 0 9.6px 28.8px rgba(0,0,0,0.18);
  align-self: stretch;
  display: flex;
}
.gallery img:not(.gallery-row img) {
  width: auto;
  max-width: 100%;
  height: auto;
  display: block;
  border-radius: 9.6px;
  box-shadow: 0 9.6px 28.8px rgba(0,0,0,0.18);
  max-height: 504px;
  margin-left: auto;
  margin-right: auto;
}
/* scroll hint fade */
.scroll-hint {
  transition: opacity 200ms linear;
  will-change: opacity;
}
</style>
</head>
<body>
<video id="bg-video" autoplay muted loop playsinline preload="auto">
  <source src="stop-motion2.mp4" type="video/mp4">
</video>
<div class="content">
<h1>The Archaeology of a Laptop</h1>
<p><em>An exploration into e-waste:<br>Uncovering consumption, process, and policy</em></p>
</div>

<div class="scroll-hint" style="text-align: center; position: absolute; left: 50%; top: 500px; transform: translateX(-50%);">
  <h1>&darr;</h1>
  <p>Scroll down</p>
</div>

<div style="height: 504px;"></div>
<section class="gallery" aria-label="Additional images">
  <div class="gallery-row">
    <img src="ewaste_graphics/COVER3.png" alt="Cover image">
    <img src="ewaste_graphics/components_x-ray.png" alt="Components X-ray">
  </div>
  <div class="gallery-row">
    <img src="ewaste_graphics/silvery.png" alt="Silvery illustration">
    <img src="ewaste_graphics/METROPOLIS1_high.res.png" alt="Metropolis artwork">
  </div>
  <img src="ewaste_graphics/HiddenLanguageOfQRCodes_high.res3.png" alt="Hidden language of QR codes">
</section>
<script>
// Fade out the scroll hint as the page is scrolled down.
(function(){
  const el = document.querySelector('.scroll-hint');
  if(!el) return;
  // distance over which the hint fades out (px)
  const fadeDistance = 300;
  function update(){
    const y = window.scrollY || window.pageYOffset;
    const t = Math.max(0, Math.min(1, y / fadeDistance));
    el.style.opacity = String(1 - t);
  }
  window.addEventListener('scroll', update, {passive:true});
  window.addEventListener('resize', update);
  // set initial state
  update();
})();
</script>
</body>
</html>
